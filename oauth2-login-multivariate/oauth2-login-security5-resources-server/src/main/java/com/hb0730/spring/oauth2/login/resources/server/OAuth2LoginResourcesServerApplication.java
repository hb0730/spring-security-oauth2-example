package com.hb0730.spring.oauth2.login.resources.server;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * @author bing_huang
 */
@SpringBootApplication
public class OAuth2LoginResourcesServerApplication {
    public static void main(String[] args) {
        SpringApplication.run(OAuth2LoginResourcesServerApplication.class, args);
    }
}
